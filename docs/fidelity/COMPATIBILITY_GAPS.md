# Compatibility gaps observed during the bounded bootstrap

Baseline: `main` at `2ff1d25a0d70c8e343d8e15db138cf7f30a29c55`. Target: Terraria 1.4.5.7 / Steam BuildID 24825745, pinned in [TARGET_LOCK.json](TARGET_LOCK.json). The eight source blobs listed in the root brief match this checkout. This is an evidence-backed gap inventory, not authorization to change the generator, art or UI.

| Area | Current local implementation | Pinned reference evidence | Consequence / later work |
|---|---|---|---|
| Seed conversion | `terraexplorer/config.py:36-52`, `seed_to_uint32`: low unsigned 32 bits for numbers; strips text before CRC32 | `IO/WorldFileData.cs:393-410`: Int32 parsing, absolute value, MinValue special case; `232-286` parses copied seed settings | Even negative numeric seeds differ. Text encoding/overflow and secret seed handling need primitive fixtures. |
| RNG | `terraexplorer/pipeline.py:38-41,80`: BLAKE2s(seed:pass label), then NumPy default_rng; a local check reports PCG64 | `WorldGen.cs:4597`: genRand aliases Main.rand; `WorldGenerator.cs:488-517` resets UnifiedRandom to the same world seed per enabled pass and consumes a final diagnostic draw; Dungeon also creates local settings-seeded RNGs | Neither a single continuous global-stream assumption nor the project's pass-hashed streams match this target. Preserve object identity, seed derivation, local states and branch-specific draws. |
| Pass ordering | `terraexplorer/passes.py:3-6,291-294` fixes a 107-pass older project contract; `pipeline.py:73-95` allows absent handlers | `WorldGen.AddPasses:11939-23296` has 109 registration sites, including conditional registrations; `DisablePassesForSpecialSeeds:23298-23314` also affects enabled state | Ordinary static selection yields 106 registrations. This is not a runtime trace or a universal count. See PASS_REGISTRATION.json. |
| Terrain | `terraexplorer/generation.py:174-186`: broad/fine smooth noise plus sinusoidal component and clipping | `WorldGen.cs:11941` registers TerrainPass; reference TerrainPass is a separately indexed implementation | Confirmed custom project model. TerrainPass internals and same-seed terrain comparison were not audited here. |
| Dungeon | `generation.py:1073-1127`: carved rectangular rooms and connecting project corridors, requested Small count 32 with an early-break path | `DungeonCrawler.MakeDungeon:344-534` plus local room/hall/entrance implementations and ordered feature finishing | Geometry, placement, transient state and finishing are not a port of the reference. See DUNGEON.md. |
| Temple | `generation.py:1340-1420`: Small envelope 190 by 120, room rows and separate final chamber | `WorldGen.makeTemple:34514-35400`, `templePart2:35686-35894`, later altar pass `22724-22755` | Exact geometry and multi-stage state are missing. Do not retrofit the fixed project envelope as a vanilla boundary. |
| Living trees | `generation.py:1279-1327`: solid trunk, ellipse canopy and root walks | `GrowLivingTree:28934-29570`, passage/tunnel/room helpers through `30314`; caller at `16023-16248` | The audited handler lacks the connected vanilla passage/room pipeline. Later project-pass mutations were not exhaustively ruled out. |
| Tile/wall IDs and presence | `terraexplorer/model.py:54-84`: uint8 project tile/wall arrays; `tiles.py:1-16` explicitly uses simulation IDs and AIR=0 | `Tile.cs:8-24,594-626`: ushort IDs, independent presence and inactive bits | Existing arrays cannot serve as lossless canonical Terraria data. Preserve artwork by mapping canonical state to the palette separately. |
| Shape and physical objects | `model.py:54-84` lacks explicit slopes/half-bricks, wire/actuator state, object frames and paint/coating fields | `Tile.cs:442-743`; `WorldGen.cs:22724-22755` writes framed altar/support cells; `TileObject.cs:174-887` uses object metadata | Require a canonical representation and verified import/export, not new palette IDs alone. |
| Placement guards | `generation.py:75-101` compares marker rectangles; the only `_can_place_structure` call is in the buried-chest/cabin handler at line 1540 | `StructureMap.cs:19-75,104-124` distinguishes placement tiles/protected structures; Temple/tree have separate local predicates | No universal overlap/protection or placement parity. Establish each routine's actual checks. |
| Tests and diagnostics | `tests/test_pipeline.py:34-44` proves project repeatability and selected array invariants; feature checks start at `119`; `model.py:49` changed_tiles is optional and pipeline does not fill it | No generated golden world from this run; no canonical per-cell comparison or pre/post RNG fixture | Existing test passing is project regression evidence only. No vanilla fidelity test passed. |
| Runtime oracle | Pinned server immediately fails loading XNA 4.0 | Fresh-process stderr and exit 0xE0434352 in ignored audit record | Resolve the verified dependency before claiming a real reference world. Parsing, repetition and semantic determinism remain BLOCKED. |

The current renderer, builder UI, saved media, assets and all production packages were preserved. No replacement generator, model migration, import API, dependency or gameplay system was added. The next task is to obtain a real pinned reference world, then implement a canonical importer/exporter and verify Dungeon, Temple and living-tree crops in the existing art style. Deterministic routine replay follows that work.
